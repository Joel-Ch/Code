#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // Declare some variables
    int a = 1, b = 2;
    char e = 123;
    float f = 1.23f;

    // Use printf display text on the screen
    printf ("The variables are\na = %d\nb=%d\nf=%f\ne=%d", a, b, f, e);

    // Exit from main
    return 0;
}
